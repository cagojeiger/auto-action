templates:
  - name: web
    image:
      repository: nginx
      tag: latest
    configMaps:
      - name: app-config
        data:
          config.yaml: |
            server:
              port: 8080
              host: 0.0.0.0
        volumeMount:
          mountPath: /etc/config