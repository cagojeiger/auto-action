templates:
  - name: web
    image:
      repository: nginx
      tag: latest
    configMaps:
      - name: app-config
        data:
          ENV_VAR: "test-value"
          ANOTHER_VAR: "another-value"