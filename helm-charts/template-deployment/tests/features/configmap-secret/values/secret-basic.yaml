templates:
  - name: web
    image:
      repository: nginx
      tag: latest
    secrets:
      - name: api-keys
        stringData:
          API_KEY: "secret-key-123"
          DB_PASSWORD: "secure-password"